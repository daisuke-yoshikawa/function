<!DOCTYPE html>
<html>
  <head>
    <title>blog_function</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width"> 
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
    <header>
        <ul class="row">
            <li class="col-xs-12 col-md-2"><%= link_to 'ホーム', root_path %></li>
            <li class="col-xs-12 col-md-2"><%= link_to '一覧', blogs_path %></li>
            <li class="col-xs-12 col-md-2"><%= link_to '新規投稿', new_blog_path %></li>
        <% if logged_in? %>
            <li class="col-xs-12 col-md-2"><%= link_to 'あなたの情報', user_path(current_user.id) %></li>
            <li class="col-xs-12 col-md-2"><%= link_to 'ログアウト', session_path(current_user.id), method: :delete %></li>
        <% else %>
            <li class="col-xs-12 col-md-2"><%= link_to 'ユーザー新規登録', new_user_path %></li>
            <li class="col-xs-12 col-md-2"><%= link_to 'ログイン画面', new_session_path %></li>
        <% end %>
        </ul>
    </header>
    </div>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>

    <%= yield %>

      <footer class="footer">
        <p>ドローンは世界を変える</p>
      </footer>
      
  </body>
</html>
